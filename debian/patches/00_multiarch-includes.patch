--- a/libcurl.pc.in
+++ b/libcurl.pc.in
@@ -29,6 +29,7 @@
 includedir=@includedir@
 supported_protocols="@SUPPORT_PROTOCOLS@"
 supported_features="@SUPPORT_FEATURES@"
+host_prefix=@DEB_HOST_MULTIARCH@

 Name: libcurl
 URL: http://curl.haxx.se/
@@ -36,4 +37,4 @@
 Version: @CURLVERSION@
 Libs: -L${libdir} -lcurl
 Libs.private: @LIBCURL_LIBS@
-Cflags: -I${includedir} @CPPFLAG_CURL_STATICLIB@
+Cflags: -I${includedir} -I${includedir}/${host_prefix} @CPPFLAG_CURL_STATICLIB@
--- a/configure.ac
+++ b/configure.ac
@@ -175,6 +175,11 @@
 INITIAL_LDFLAGS=$LDFLAGS
 INITIAL_LIBS=$LIBS

+dnl Get MULTILIB debian path from a user-defined
+dnl environment variable
+
+AC_ARG_VAR(DEB_HOST_MULTIARCH, Debian multiarch dir prefix)
+
 dnl
 dnl Detect the canonical host and target build environment
 dnl
--- a/include/curl/curl.h
+++ b/include/curl/curl.h
@@ -31,7 +31,7 @@
  */

 #include "curlver.h"         /* libcurl version defines   */
-#include "curlbuild.h"       /* libcurl build definitions */
+#include <curl/curlbuild.h>  /* libcurl build definitions */
 #include "curlrules.h"       /* libcurl rules enforcement */

 /*
--- a/curl-config.in
+++ b/curl-config.in
@@ -24,6 +24,7 @@
 prefix=@prefix@
 exec_prefix=@exec_prefix@
 includedir=@includedir@
+host_include=@includedir@/@DEB_HOST_MULTIARCH@
 cppflag_curl_staticlib=@CPPFLAG_CURL_STATICLIB@

 usage()
@@ -130,9 +131,9 @@

     --cflags)
         if test "X$cppflag_curl_staticlib" = "X-DCURL_STATICLIB"; then
-          CPPFLAG_CURL_STATICLIB="-DCURL_STATICLIB "
+          CPPFLAG_CURL_STATICLIB="-DCURL_STATICLIB -I$host_include "
         else
-          CPPFLAG_CURL_STATICLIB=""
+          CPPFLAG_CURL_STATICLIB="-I$host_include "
         fi
         if test "X@includedir@" = "X/usr/include"; then
           echo "$CPPFLAG_CURL_STATICLIB"
